---
name: readmap
description: >-
  Deep-read an academic paper into a decision, not a report. Use whenever the
  user asks to 精读 / deep-read / properly read a paper, wants a structured
  reading note, asks "这篇论文值不值得信", wants figures and tables pulled out of
  a paper for reading, or wants a reading note filed to Notion. Reconstructs why
  the problem exists before describing the solution, isolates the smallest thing
  the authors actually added, separates what the experiments proved from what
  merely accompanied them, and ends in one decision. Keywords: 精读, deep dive,
  paper reading, reading note, literature note, 论文精读, arxiv 精读, ReadMap.
---

# ReadMap — 论文精读

> 精读不是把论文翻译成更专业的中文，而是把它拆到最基本的问题，看清它究竟解决了
> 什么、为什么有效、证据够不够，以及有没有用复杂包装掩盖一个普通技巧。

**唯一判断标准**：遮住标题、作者、会议和所有新术语后，仅凭问题、机制和证据，还能
不能判断这个工作值不值得相信？若只剩「用了一个复杂框架，在很多数据集上涨了」，说明
还没看穿。

---

# 第 0 步 · 先抠图表（不是可选项）

**在写任何一个字之前先做这一步。** 图表是第 6、7 问的证据来源；先写完正文再补图，
必然补成图廊。

有 PDF（用户挂了附件，或你能下到）就直接对 PDF 跑。**不要先折腾 HTML 或浏览器。**

```bash
pip install git+https://github.com/Sunrich-HT/ReadMap
readmap read <PDF 路径或论文链接>
```

只要图不要骨架：

```bash
pip install git+https://github.com/Sunrich-HT/figure-extractor
figure-extractor extract <PDF 路径> --out ./figures --dpi 300 --zip
```

只依赖 PyMuPDF + Pillow + beautifulsoup4，无需 GPU、模型或服务。
**包没装不是降级的理由 —— 先装。**

拿不到 PDF 时才试 arXiv HTML 版（`arxiv.org/abs/X` → `arxiv.org/html/Xv1`），嵌入
原始图片 URL。部分环境的 HTML 抓取器会剥离 `<img src>`，碰到就回去下 PDF。

真的一条都不通才降级：给出页码、图号、逐字 caption，**明写缺口原因及试过哪条路**。
绝不凭空描述没看见的图，也不要把「描述了图」写成「提取了图」。

**抠取是穷尽的，选用不是。** 全部抠出来，正文通常只用 2–4 张，其余进附件 zip。

---

# 第 1 步 · 按八问写

标了 **[图位]** 的地方是该问最可能需要配图的位置。写到那里时先问一句「这一段有没有
一张图能让人立刻看懂」——有就当场插入，没有就跳过。**不要留到最后统一插。**

### 1 · 三十秒讲清楚
解决什么？关键办法？最终判断？外加三条关键数字（每条带证据标签）。

### 2 · 第一性原理：这个问题为什么存在
**不用论文任何术语。** 世界里原本有什么矛盾？哪两件事物理上不可兼得？
不先说清这个矛盾，就说不出这篇论文为什么需要存在。

### 3 · 一个能在脑中运行的例子
拆掉术语 → 最小玩具世界 → 回到严格表达。
每个比喻必须补三句：对应什么数学对象、在哪里成立、在哪里会误导。
**[图位]** 论文的总览图 / 框架图放这里——它的作用就是让人一眼看懂全局。

### 4 · 作者真正新增了什么
去掉系统包装，只留最小创新单元。新增了哪个自由度，又带来什么新风险？
**[图位]** 方法图、架构图、任务设计表放这里，紧跟你描述的那个创新单元。

### 5 · 为什么理论上应该有效
机制解释，把直觉、公式、假设对应起来。只说结果涨了不算回答。

### 6 · 证明了什么、没证明什么
直接证据 / 间接证据 / 作者的推断 / 我的推断 / 尚未验证，五类分开列。
**[图位]** 主结果表、消融表、尺度曲线放这里，**紧跟引用它的那一条**，不要堆在节末。

### 7 · 承重审计：哪根梁真正承重
作者真正想卖什么？为证明它搭了多少外围系统？去掉核心创新其他不变会掉多少？
有没有更便宜的替代？增益是否只来自参数量或数据量？叙事是否超过证据？

收口到三问，不要罗列通用局限：
只能推翻一个假设该推翻哪个？哪个结果消失论文就坍塌？最小判决实验是什么？
**[图位]** 支撑「承重」判断的那张关键图放这里。

### 8 · 最终判决
择一：真突破 / 扎实增量 / 工程整合 / 评测贡献 / 有趣但证据不足 / 包装大于贡献 /
当前无法判断。并写**主张降级**：作者说 X，证据只支持到 Y。

下一步**只能勾一项**：引用 / 改 claim / 改实验 / 登记不行动 / 最小验证实验。
一项都没产生时，`closure` 只能是「阅读完成」。

## 三条贯穿全文的硬规则

**一 · 每个关键数字都带证据标签**，说明「我是怎么知道的」：
`[Paper/Fig N]` `[Paper/Table N]` `[Appendix X.Y]` `[Code inspected]`
`[Recomputed]` `[My inference]` `[Unverified]`
无标签的数字一律按 `[Unverified]` 计。**「读过完整 PDF」不等于「关键数字已被独立核验」。**

**二 · 与我研究的关系默认 none。** 先回答「完全不考虑当前项目，这篇论文的独立价值
是什么」。只有能指出进入正文哪一节（`cite`）、能说出改哪个对照或指标（`design`）、
能指出我的哪个假设可能失效（`warning`）、或承认只是思考素材（`analogy`），才能改。
判断「不相关」不是失败。

**三 · 结论只写一处。** 开头一句话判决、中间证据链、结尾一个决策。速查卡交给数据库
字段，正文不要再写一遍。

## 图放在哪里

**图是用来解释文字的，所以它必须挨着它解释的那句话。**

- **就近**：紧跟它支撑的那句话，不放章节末尾，不集中成组。
- **一图一用**：每张图只出现一次，在它最承重的位置。
- **图注写它在这里干什么**，不是复述 caption。
- **连续两张图之间必须有解释文字。** 硬规则。
- 放不进任何论述旁边的图 → 它在这篇精读里不承重，**移出正文进附件**。

**不要设「关键图表解读」这类独立章节。** 那是图廊的另一个名字。

---

# 第 2 步 · 交付前自检（必须输出）

写完后**在正文末尾输出这个块**，逐项填真实数字。填不出来就是没做，回去做。

```
自检
- 图表抠取：共 __ 项（图 __ / 表 __），方式：readmap / figure-extractor / HTML / 降级
- 正文用图：__ 张，全部紧邻其论述？ 是 / 否
- 连续两张图之间无文字：__ 处（必须为 0）
- 独立图表章节：无 / 有（有则删掉，图归位）
- 关键数字带证据标签：__ / __ 条
- 证据等级：L__ ，理由：____（没打开过代码就不是 L4）
- 下一步勾选：__ 项（必须为 1）
- 评分：__ / __ 制（子项与总分同制式）
```

本地可跑 `readmap gate <文件>` 做同样的检查：它会阻断图廊、生成残留、标了闭环却无
决策，并提示脱节的图、缺标签的数字、超出制式的评分、三处重复的结论。

## 必填的文献库字段

| 字段 | 取值 |
| --- | --- |
| 文档类型 | 单篇精读 / Radar 综述 / 自主教程 / 复现报告 |
| 证据等级 | L1 摘要 / L2 原文 / L3 图表附录 / L4 代码数据 / L5 局部复现 |
| 项目关系 | **none**（默认）/ cite / design / warning / analogy |
| 关系理由 | 关系不为 none 时必填 |
| 决策闭环 | 阅读完成 / 证据核验完成 / 复现完成 / 迁移实验完成 / 已进入论文 |
| 最终判决 | 上面七选一 |
| Reviewer评分 + 评分制式 | 必须同时给，4/5 与 4/10 不是一回事 |
| 速查卡 | 一句话判决放这里，正文不要再写 |

**证据等级与篇幅无关**：L1 可以很长，L4 可以很短。不确定就填低不填高。
